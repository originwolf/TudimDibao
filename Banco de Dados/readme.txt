Do not modify the tables structre, unless you know how to fix the PHP files.
If you rename the database, change the PHP files, so they can find the right database.
Don't create new indexes on the "cardapio" table. The PHP file will only update the index "0" and print it on the screen.
